import { nanoid } from 'nanoid';
import { z } from 'zod';

/** Generates a default value */
const nanoidSchema = z
    .string()
    .min(7)
    .optional()
    .default(() => nanoid());

export { nanoidSchema };
